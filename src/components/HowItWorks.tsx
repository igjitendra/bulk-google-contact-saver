import { Upload,CheckCircle2,CloudUpload } from 'lucide-react';
const steps=[{icon:Upload,title:'Import',text:'Upload a CSV or paste a list.'},{icon:CheckCircle2,title:'Clean & Review',text:'Normalize +91 numbers and resolve issues.'},{icon:CloudUpload,title:'Save to Google',text:'Create selected contacts securely.'}];
export default function HowItWorks(){return <section><div className="mb-5"><p className="text-sm font-semibold text-accent">THREE SIMPLE STEPS</p><h2 className="text-2xl font-semibold text-ink">From messy list to Google Contacts</h2></div><div className="grid gap-4 md:grid-cols-3">{steps.map((s,i)=><article key={s.title} className="rounded-xl border border-line bg-white p-5"><div className="mb-4 flex items-center justify-between"><s.icon className="h-6 w-6 text-accent"/><span className="text-sm text-muted">0{i+1}</span></div><h3 className="font-semibold text-ink">{s.title}</h3><p className="mt-1 text-sm text-muted">{s.text}</p></article>)}</div></section>}
