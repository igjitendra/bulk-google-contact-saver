'use client';import { SaveProgress } from '@/lib/types';
export default function BatchProgressModal({isOpen,progress}:{isOpen:boolean;progress:SaveProgress}){if(!isOpen)return null;const pct=progress.total?Math.round(progress.current/progress.total*100):0;return <div className="modal-backdrop"><div className="modal-card"><div><p className="text-sm font-semibold text-accent">SAVING CONTACTS</p><h2 className="text-xl font-semibold">{pct}% complete</h2></div><div><div className="h-2 overflow-hidden rounded bg-[#F0EFED]"><div className="h-full bg-accent transition-all" style={{width:`${pct}%`}}/></div><p className="mt-2 truncate text-sm text-muted">{progress.currentContactName||'Preparing…'}</p></div><div className="grid grid-cols-3 gap-3 text-center text-sm"><div className="rounded-lg bg-[#E8F1EC] p-3"><strong>{progress.saved}</strong><br/>Saved</div><div className="rounded-lg bg-[#FCE9E7] p-3"><strong>{progress.failed}</strong><br/>Failed</div><div className="rounded-lg bg-[#F9F8F7] p-3"><strong>{progress.current}/{progress.total}</strong><br/>Processed</div></div></div></div>}
