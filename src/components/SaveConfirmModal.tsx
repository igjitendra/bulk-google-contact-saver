'use client';import { CloudUpload,X } from 'lucide-react';
export default function SaveConfirmModal({isOpen,count,userEmail,onConfirm,onClose}:{isOpen:boolean;count:number;userEmail?:string|null;onConfirm:()=>void;onClose:()=>void}){if(!isOpen)return null;return <div className="modal-backdrop"><div className="modal-card"><div className="flex justify-between"><CloudUpload className="h-9 w-9 text-accent"/><button aria-label="Close" onClick={onClose}><X/></button></div><h2 className="text-xl font-semibold">Save {count} contacts?</h2><p className="text-sm text-muted">Contacts will be created in <strong>{userEmail||'your connected Google account'}</strong>. This may take a moment.</p><div className="flex justify-end gap-2"><button className="btn-secondary" onClick={onClose}>Cancel</button><button className="btn-primary" onClick={onConfirm}>Confirm and save</button></div></div></div>}
