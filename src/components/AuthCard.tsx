'use client';
import Image from 'next/image';import { signIn,signOut,useSession } from 'next-auth/react';import { LogIn,LogOut } from 'lucide-react';
export default function AuthCard(){const {data,status}=useSession();if(status==='loading')return <div className="rounded-xl border border-line bg-white p-4 text-muted">Checking Google connection…</div>;return <section className="flex flex-col gap-4 rounded-xl border border-line bg-white p-5 sm:flex-row sm:items-center sm:justify-between"><div>{data?.user?<div className="flex items-center gap-3">{data.user.image&&<Image className="rounded-full" src={data.user.image} width={40} height={40} alt="Google profile"/>}<div><p className="font-medium text-ink">Connected to Google</p><p className="text-sm text-muted">{data.user.email}</p></div></div>:<div><p className="font-medium text-ink">Connect your Google account</p><p className="text-sm text-muted">Required only when you are ready to save.</p></div>}</div>{data?<button onClick={()=>signOut()} className="btn-secondary"><LogOut className="h-4 w-4"/>Sign out</button>:<button onClick={()=>signIn('google')} className="btn-primary"><LogIn className="h-4 w-4"/>Sign in with Google</button>}</section>}
