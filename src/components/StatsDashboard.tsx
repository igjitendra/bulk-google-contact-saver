'use client';import { ImportStats } from '@/lib/types';import { RefreshCw } from 'lucide-react';
export default function StatsDashboard({stats,onCheckExisting,isCheckingExisting}:{stats:ImportStats;onCheckExisting?:()=>void;isCheckingExisting:boolean}){const cards=[['Imported',stats.totalImportedRows],['Valid',stats.validContacts],['Needs attention',stats.invalidContacts],['Duplicates removed',stats.duplicatesRemoved],['Ready to save',stats.readyToSave]];return <section className="rounded-xl border border-line bg-white p-5"><div className="grid gap-3 sm:grid-cols-5">{cards.map(([l,v])=><div key={l} className="rounded-lg bg-[#F9F8F7] p-4"><p className="text-2xl font-semibold text-ink">{v}</p><p className="text-xs text-muted">{l}</p></div>)}</div>{onCheckExisting&&<button className="btn-secondary mt-4" onClick={onCheckExisting} disabled={isCheckingExisting}><RefreshCw className={`h-4 w-4 ${isCheckingExisting?'animate-spin':''}`}/>{isCheckingExisting?'Checking…':'Check existing in Google'}</button>}</section>}
