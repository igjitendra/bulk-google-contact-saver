'use client';import { useState } from 'react';import { FileSpreadsheet,ClipboardPaste } from 'lucide-react';import CsvUploader from './CsvUploader';import CopyPasteArea from './CopyPasteArea';import { RawContactInput } from '@/lib/types';
export default function ImportTabs({onParsed}:{onParsed:(r:RawContactInput[])=>void}){const [tab,setTab]=useState<'csv'|'paste'>('csv');return <section className="rounded-xl border border-line bg-white p-5"><div className="mb-5 flex border-b border-line"><button onClick={()=>setTab('csv')} className={`tab ${tab==='csv'?'tab-active':''}`}><FileSpreadsheet className="h-4 w-4"/>CSV upload</button><button onClick={()=>setTab('paste')} className={`tab ${tab==='paste'?'tab-active':''}`}><ClipboardPaste className="h-4 w-4"/>Copy & paste</button></div>{tab==='csv'?<CsvUploader onParsed={onParsed}/>:<CopyPasteArea onParsed={onParsed}/>}</section>}
