import { ProcessedContact } from './types';
const esc=(v:unknown)=>`"${String(v??'').replace(/"/g,'""')}"`; const download=(name:string,rows:string[][])=>{const csv=rows.map(r=>r.map(esc).join(',')).join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv;charset=utf-8'}));a.download=name;a.click();URL.revokeObjectURL(a.href);};
export const downloadSampleCsv=()=>download('sample-contacts.csv',[['Phone','Name'],['9876543210','Aarav Sharma'],['+91 87654 32109','Diya Patel'],['0091-76543-21098','Kabir Singh'],['6543210987','Meera Rao']]);
export const downloadCompleteReportCsv=(c:ProcessedContact[])=>download('contact-save-report.csv',[['Row','Name','Phone','Status','Error'],...c.map(x=>[String(x.originalRowIndex),x.finalName,x.googleInternationalPhone,x.status,x.saveError||''])]);
export const downloadFailedContactsCsv=(c:ProcessedContact[])=>download('failed-contacts.csv',[['Name','Phone','Error'],...c.filter(x=>x.status==='failed').map(x=>[x.finalName,x.googleInternationalPhone,x.saveError||''])]);
